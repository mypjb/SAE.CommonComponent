{
  "name": "authorize",
  "version": "1.0.0",
  "scripts": {
    "dev": "cross-env UMI_ENV=dev max dev",
    "build": "max build",
    "format": "prettier --cache --write .",
    "postinstall": "max setup",
    "setup": "max setup",
    "start": "npm run dev"
  }
}
